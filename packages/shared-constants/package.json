{
  "name": "shared-constants",
  "version": "0.0.0",
  "description": "Shared constants package",
  "main": "src/index.ts",
  "files": [
    "src/index.ts",
    "src/types.ts"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "yarn build --watch"
  },
  "dependencies": {
    "ethers": "5.7.2"
  },
  "devDependencies": {
    "config": "*",
    "tsconfig": "*"
  }
}
